#ifndef CS348_BENCH_H
#define CS348_BENCH_H

// Define a few benchmarking constants to use when benchmarking. We'll
// benchmark at a higher resolution than the images in our dataset.
const int IMAGE_WIDTH = 1024;
const int IMAGE_HEIGHT = 1024;

#endif // CS348_BENCH_H